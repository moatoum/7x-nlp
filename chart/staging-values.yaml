spc:
  enabled: false

objects: |
     array:  
        - |
          objectType: secret              # object types: secret, key, or cert  
        

hpa:
  enabled: true
  minReplicas: 1
  maxReplicas: 2
  targetMemoryUtilizationPercentage: 75
resources:
  requests:
    memory: "200Mi"
  limits:
    memory: "512Mi"